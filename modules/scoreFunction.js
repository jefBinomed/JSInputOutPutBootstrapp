'use strict'

/**
 *
 * @param {Object} input
 * @param {Object} output
 * @returns {number}
 */
function computeScore(input, output){
    let score = 0;

    return score;
}

module.exports = computeScore;